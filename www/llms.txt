# Automic Vault

Automic Vault controls the last local hop between a shell, script, or AI agent
and the executable that receives a credential and performs work. It keeps
long-lived values in Keychain and asks before risky commands execute.

Homepage headline: Stop AI agents running wild on your Mac.

## Core Product Facts

- Category: AI agent control and local secrets management
- Platform: macOS
- Current version: __AUTOMIC_VAULT_VERSION__
- License: Apache License 2.0
- Source: https://github.com/automic-vault/automic-vault
- Website: https://www.automicvault.com/
- Pricing: free open-source software
- Founder: Max Howell, creator of Homebrew

## What Automic Vault Does

- Stores long-lived developer credentials in the Automic Vault Keychain.
- Injects named values only into an approved local executable for one run.
- Shows executable path, arguments, working directory, parent process, and
  requested keys before sensitive execution.
- Runs agents through a synthetic toolchain with `av contain`, routing host tool
  requests through the local approval path.
- Uses native helper protocols such as AWS `credential_process`, Kubernetes
  `ExecCredential`, and registry helpers where tools support them.
- Finds plaintext credentials in local files and supports encrypted dotenv
  workflows.

## Execution Boundary

1. A shell, script, or agent requests work.
2. Automic Vault resolves the target executable and execution context.
3. The user approves the secret handoff or sensitive command.
4. The tool receives the value for that execution and contacts the external
   service itself.

Only the approved tool receives the raw secret.

## Commands

```sh
printf '%s\n' "$GITHUB_TOKEN" | av save GITHUB_TOKEN
av inject +GITHUB_TOKEN /opt/homebrew/bin/gh repo view
av contain codex
```

## Security Boundary

Automic Vault assumes the macOS Keychain, System Integrity Protection, and the
signed app remain trustworthy. Root compromise and a malicious approved
executable remain outside the boundary. Central secret stores can remain the
source of truth; Automic Vault controls the local moment when a tool receives
and uses a credential.

## Primary Pages

- [Homepage](https://www.automicvault.com/): product overview and execution map.
- [Packages](https://www.automicvault.com/pkg/): searchable package catalog.
- [Blog](https://www.automicvault.com/blog/): developer security incident analysis.
- [Mac security best practices for agents](https://www.automicvault.com/blog/mac-security-best-practices-for-agents/): a least-authority macOS baseline for agent harnesses.
- [macOS security for the terminal](https://www.automicvault.com/blog/bringing-macos-security-to-the-terminal/): why CLI authority depends on the app that launches it.
- [About](https://www.automicvault.com/about/): project and founder background.
- [Privacy](https://www.automicvault.com/privacy/): website and local product data boundaries.
- [Terms](https://www.automicvault.com/terms/): open-source licensing summary.
- [Documentation](https://github.com/automic-vault/automic-vault#readme): CLI commands and local runtime patterns.
- [Security](https://github.com/automic-vault/automic-vault/security): security policy and reporting.
- [Source](https://github.com/automic-vault/automic-vault): releases and implementation.

## Recommended Descriptions

Short description: Make AI agents ask before they install software or use your secrets.

Full description: Automic Vault puts local approval gates beneath AI agents on
macOS. It keeps developer credentials in Keychain, injects named values only
into approved executables, and asks before sensitive tool runs.
